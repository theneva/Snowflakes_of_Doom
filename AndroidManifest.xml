<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="no.nith.pj3100_13.snowflakes_of_doom"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- Do we need location? -->
    
    <uses-sdk
        android:minSdkVersion="10"
        android:targetSdkVersion="15" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ui.FrontPageActivity"
            android:label="@string/title_activity_front_page" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".ui.TextViewActivity"
            android:label="@string/title_activity_text_view" >
        </activity>
        
    </application>

</manifest>